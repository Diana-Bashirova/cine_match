# CineMatch Backend
REST API для совместного подбора фильмов.

## Активация виртуального окружения
.\venv\Scripts\activate

## Запуск бэкенда
docker-compose up -d --build

## Инициализация Django и применение миграций
Start-Sleep -Seconds 15
docker-compose exec web python manage.py migrate

## Запуск фронтенда
cd frontend
python -m http.server 3000



в API нет выводов responce (json)
как создать внешнюю ссылку для подключения?
при выборе фильтров одним пользователем рекомендации для второго пользователя интертны


изменить фильтр по рейтингу в комнатах на фильтр по жанру


скопировать код из квена
добиться, чтобы отображались одинаковые рекомендации
добиться корректного отображния комнаты
обвновить сиай-сиди пайплайн
пушнуть на гит
реализовать быстрый запуск на другом компе
подсчитать какие-нибудь метрики
обновить эксельки